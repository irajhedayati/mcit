package ca.mcit.bigdata.course4.project.model;

/**
 * Complete this POJO to represent a record of stop times read from STM source.
 * Each attribute cossorponds a field in {@code stop_times.txt} and must have a getter and a setter
 *
 * Also, there is a static helper method called {@link StopTime#parseStopTime(String)} has to be implemented.
 * Check its JavaDoc for more details
 *
 * @author iraj
 * @since 9/16/18
 */
public class StopTime {

    /**
     * Gets a line from a CSV file and parses it to an object of {@link StopTime}.
     */
    public static StopTime parseStopTime(String record) {
        return null;
    }
}
